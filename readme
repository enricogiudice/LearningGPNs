sampling_fns.R contains the main functions for BGe & GP score-based structure learning.

GP_sims.R contains the code for running the main simulation study (the data for Figures 1, A2 & A3).

posterior.R generates Figure 2.

score_equivalence.R generates Figure A1.

The .Stan files contain code for the Gaussian, GP & GP^2 models.

Sachs.R generates the data for Table 1.

comparison_algs.R contains functions for k_PC, DiBS+ and comparing the results.

Fourier_fns.R contains functions to generate data according to Equation (15).

Results/ contains the collected data from the GP_sims.R and Sachs.R;
Plots/ contains all generated plots and the code used to create Figures 1, A2 & A3.
